import React from 'react'
import { useRefreshStyle } from '../../../hooks/useRefreshStyle'

const RefreshStyle = () => {
    useRefreshStyle()
    return (
        <div>Refresh style page</div>
    )
}

export default RefreshStyle
