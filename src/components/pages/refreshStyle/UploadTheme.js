import React from 'react'
import { useUploadTheme } from '../../../hooks/useUploadTheme'

const UploadTheme = () => {
    useUploadTheme()
    return (
        <div>Upload theme page</div>
    )
}

export default UploadTheme
